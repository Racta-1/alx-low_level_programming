#include "main.h"
#include <stdio.h>
/**
 * mul - a function that multiplies two integers
 * @a: input integer number
 * @b: input inter number
 *
 * Return: return the result
 */

int mul(int a, int b)
{
	return (a * b);
}
